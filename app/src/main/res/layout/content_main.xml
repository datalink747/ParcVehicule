<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"

    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.project.parcvehicule.MainActivity"
   >
   <!-- tools:showIn="@layout/activity_main"-->

    <Button
        android:layout_marginTop="30dp"
        android:id="@+id/btn_mission"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textColor="#fff"
        android:textSize="20dp"
        android:elevation="17dp"
        android:fontFamily="sans-serif-ligt normal"
        android:text="Mission"
        android:drawableLeft="@drawable/logo1"
        android:theme="@style/MyButton"/>

    <Button
        android:layout_marginTop="20dp"
        android:id="@+id/btn_intervention"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableLeft="@drawable/logo1"
        android:textStyle="bold"
        android:textColor="#fff"
        android:textSize="20dp"
        android:elevation="17dp"
        android:fontFamily="sans-serif-ligt normal"
        android:text="Intervention"
        android:layout_below="@+id/btn_mission"
        android:theme="@style/MyButton"/>

    <Button
        android:layout_marginTop="20dp"
        android:id="@+id/btn_propos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableLeft="@drawable/logo1"
        android:textStyle="bold"
        android:textColor="#fff"
        android:textSize="20dp"
        android:elevation="17dp"
        android:fontFamily="sans-serif-ligt normal"
        android:text="A propos"
        android:layout_below="@+id/btn_intervention"
        android:theme="@style/MyButton"/>

</RelativeLayout>
